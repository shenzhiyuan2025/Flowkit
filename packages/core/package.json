{
  "name": "@flowkit/core",
  "version": "0.1.0",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsup src/index.ts --dts --format esm",
    "test": "vitest run",
    "lint": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@flowkit/protocol": "file:../protocol"
  },
  "devDependencies": {
    "tsup": "^8.3.5",
    "typescript": "^5.8.2",
    "vitest": "^4.1.0"
  }
}
